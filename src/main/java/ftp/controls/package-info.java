/**
 * Package that contains all the controllers for every FTP command that the
 * server supports. These control objects are invoked when the server received
 * the right command.
 */
package ftp.controls;